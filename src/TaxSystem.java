//Таким образом, с начала 2025 года для физических лиц с годовым доходом до 2,4 млн руб. включительно (в среднем не больше 200 тыс. руб. в месяц) будет действовать ставка 13%.
//Ставка 15% — для части годового дохода выше 2,4 млн руб. и до 5 млн руб. включительно (в среднем выше 200 тыс. руб. и до 416,7 тыс. руб. включительно в месяц).
//Ставка 18% — для части годового дохода выше 5 млн руб. и до 20 млн руб. включительно (в среднем выше 416,7 тыс. руб. и до 1,670 тыс. руб. включительно в месяц).
//Ставка 20% — для части годового дохода выше 20 млн руб. и до 50 млн руб. включительно (в среднем выше 1,670 тыс. руб. и до 4,170 тыс. руб. включительно в месяц).
//Ставка 22% — для части дохода выше 50 млн руб. в год, или выше 4,170 тыс. руб. в месяц
public class TaxSystem {

    // Переменные для расчета зарплаты и налога
    private double accruedSalary = 50_000; // Начисленная зарплата
    private double tax = 0; // Сумма налога
    private double paidSalary = 0; // Выданная зарплата

    // Метод для расчета налогов в зависимости от дохода
    public double calculateTax(double annualIncome) {
        double tax = 0;

        if (annualIncome <= 2400000) {
            tax = annualIncome * 0.13; // 13%
        } else if (annualIncome <= 5000000) {
            tax = (2400000 * 0.13) + ((annualIncome - 2400000) * 0.15); // 15%
        } else if (annualIncome <= 20000000) {
            tax = (2400000 * 0.13) + ((5000000 - 2400000) * 0.15) + ((annualIncome - 5000000) * 0.18); // 18%
        } else if (annualIncome <= 50000000) {
            tax = (2400000 * 0.13) + ((5000000 - 2400000) * 0.15) + ((20000000 - 5000000) * 0.18) + ((annualIncome - 20000000) * 0.20); // 20%
        } else {
            tax = (2400000 * 0.13) + ((5000000 - 2400000) * 0.15) + ((20000000 - 5000000) * 0.18) + ((50000000 - 20000000) * 0.20) + ((annualIncome - 50000000) * 0.22); // 22%
        }

        return tax;
    }

    // Метод для расчета налога (без реализации)
    public double getTax() {
        // Реализация будет здесь
        return 0;
    }

    // Метод для расчета зарплаты к выдаче на руки работнику (без реализации)
    public double getPaidSalary() {
        // Реализация будет здесь
        return 0; // заглушка
    }

    public static void main(String[] args) {
        TaxSystem taxSystem = new TaxSystem();

        // Пример доходов для расчета налога
        double[] incomes = {2000000, 3000000, 6000000, 30000000, 70000000};

        for (double income : incomes) {
            double tax = taxSystem.calculateTax(income);
            System.out.printf("Годовой доход: %.2f руб. Налог: %.2f руб.%n", income, tax);
        }
    }
}
